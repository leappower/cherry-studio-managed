# CherryStudio 企业受管版 · 完整方案 v3.1

> 状态: 方案定稿 | 最后更新: 2026-08-07
> 负责人: 鮱澄军团（主 Agent 调度）
> 目标: 公司内部 CherryStudio 企业受管分发系统（补丁包 + Web后台 + 后端拉取 + 升级自愈 + 智能体/设置全维管控）

---

## 〇、本版与 v3.0 的差异（智能体 + 设置页管控细化）

v3.0 之后老板细化了两大块需求（智能体功能、设置页管控），v3.1 全部落地：

| 决策 | 内容 | 影响 |
|------|------|------|
| ① Git Bash 依赖 | CherryStudio 智能体依赖 Git Bash；服务端能看到每台设备是否已装，缺则管理员可远程装最新版 | 智能体可用性保障 |
| ② Agent 不可删 | 远程派发的智能体，员工端删除按钮隐藏 | 受管 Agent 不可被员工删除 |
| ③ Agent 全维控制 | 远程派发智能体的基础设置/提示词/工具/技能/高级设置全部可控制 | 智能体完整管控 |
| ④ Agent 打包链路 | 调好的智能体用 SKILLS 打包成派发包 → 上传服务端 → 版本管理 → 下发 | Agent/SKILLS 版本生产闭环 |
| ⑤ 设置页删除保护 | 远程派发模型平台 / SKILLS 员工端不可删除 | 受管项不可被员工删除 |

**核心架构转变**：在 v3.0「补丁包 + 后端拉取」基础上，补齐「智能体生产打包链路」和「设置页受管项删除保护」两大闭环。

---

## 〇〇、v3.0 已确认内容（本版承接）

> v3.0 的全部章节（目标/需求/架构/补丁包/锁死UI/升级自愈/服务端/通信协议/核心流程/设计决策/技术选型/里程碑/决策点/待求证/交付物）均为 v3.1 的基础，v3.1 在其上追加新需求。以下为 v3.0 核心决策回顾（完整内容见本文件后文）：

| 决策 | 内容 |
|------|------|
| ① 交付形态 | **补丁安装包**（员工装官方版 + 打补丁） |
| ② 控制端 | **Web 管理后台** |
| ③ 补丁包定位 | **只打通后端链接，不注入核心功能**；核心功能后端拉取 |
| ④ 升级策略 | **A：放任升级 + 自愈**（Sidecar 监控 + 后端补丁库 + 自动重打） |

---

v2.0 之后老板拍板了 4 个关键决策，v3.0 全部落地：

| 决策 | 内容 | 影响 |
|------|------|------|
| ① 交付形态 | **补丁安装包**（员工装官方版 + 打补丁） | 不是改造版安装包 |
| ② 控制端 | **Web 管理后台** | 浏览器操作 |
| ③ 补丁包定位 | **只打通后端链接，不注入核心功能**；核心功能后端拉取 | 更新迭代成本大降 |
| ④ 升级策略 | **A：放任升级 + 自愈**（Sidecar 监控 + 后端补丁库 + 自动重打） | 员工升级不受影响 |

**核心架构转变**：从「改造版安装包」→「官方版 + 轻量补丁包 + 后端拉取迭代」。

---

## 一、目标（一句话）

给公司内部做「CherryStudio 企业受管版」：**员工装官方版 + 打一个轻量补丁包，补丁包只打通后端链接；核心功能（模型/Agent/SKILLS/锁死规则）全部由后端拉取迭代；员工端受管项锁死不能改，但可自配自己的模型；公司模型 Key 不泄露；数据回采驱动进化；员工自己升级 CherryStudio 不破坏受管状态。**

---

## 二、需求清单（R1-R7，最终版）

| # | 需求 | 强化点 | 实现层 | 覆盖 |
|---|------|--------|--------|------|
| R1 | **模型统一管控** | ①远程端派发**公司模型列表**；②可**收回**；③**员工仍可自配模型不受影响**（双向并存） | Sidecar + 后端拉取 | ✅ |
| R2 | **Key 保护** | 员工端**看不到**公司派发模型的 Key 原文 | 模型网关 / UI锁 | ✅ |
| R3 | **Agent 远程管理** | 派发 Agent **开箱即用**，支持**收回/禁用/升级**；Agent=**MCP+SKILL+预设数据文件**完整打包 | Sidecar | ✅ |
| R4 | **SKILLS 派发+版本管理** | 支持 **Bootstrap、SKILLS 版本、MCP**，可回收/升级 | Sidecar | ✅ |
| R5 | **锁死 UI** | 员工端设置页：**远程端管理的（模型/MCP/SKILLS/Agent）锁死或隐藏**；**锁死规则可远程迭代** | 注入器 + 后端规则 | ✅ |
| R6 | **数据采集** | Sidecar 采集使用数据 → 服务端分析 → 驱动进化 | Sidecar | ✅ |
| R7 | **升级免疫** | 员工自己升级**不破坏受管状态**（自愈重打补丁） | Sidecar 自愈 | ✅ |

---

## 三、总体架构（补丁包 + Web后台 + 后端拉取）

```
┌───────────── 管理服务端 (server/) ─────────────┐
│  · Web 管理后台（浏览器操作）                    │
│  · 模型 Provider 仓库（统一配置、集中发 key）     │
│  · Agent 模板库 + SKILLS 版本管理               │
│  · 锁死规则库（远程迭代 UI 锁死）                │
│  · 补丁库（按 CherryStudio 版本适配）            │
│  · 使用数据仓库（采集→分析→进化闭环）            │
│  · 派发调度 + 更新服务                          │
└──────────▲───────────────────────────▲────────┘
           │ WS 主动连接(2334)           │ WS
┌──────────┴──────────┐      ┌──────────┴──────────┐
│ 员工设备 A           │ ... │ 员工设备 N           │
│  ┌──────────────┐   │      │  ┌──────────────┐   │
│  │ CherryStudio │   │      │  │ CherryStudio │   │
│  │ (官方版)      │   │      │  │ (官方版)      │   │
│  └──────▲───────┘   │      │  └──────▲───────┘   │
│         │ 注入器     │      │         │ 注入器     │
│  ┌──────┴───────┐   │      │  ┌──────┴───────┐   │
│  │  Sidecar     │   │      │  │  Sidecar     │   │
│  │ (补丁包安装)  │   │      │  │ (补丁包安装)  │   │
│  └──────────────┘   │      │  └──────────────┘   │
└─────────────────────┘      └─────────────────────┘
```

### 三层职责分工

| 层 | 改本体? | 职责 | 覆盖需求 |
|----|--------|------|---------|
| **服务端 server/** | 否 | Web后台 + 派发调度 + 锁死规则库 + 补丁库 + 数据仓库 | 全局中枢 |
| **补丁包 patch/** | 部分 | 装 Sidecar + 注入器（一次性锁死机制）+ 受管标记 | R2/R5/R7 |
| **Sidecar sidecar/** | 否 | 后端拉取核心功能 + 派发执行 + 数据采集 + 自愈 | R1/R3/R4/R6 |

---

## 四、交付形态（关键）

### 4.1 员工安装流程
```
1. 员工去 CherryStudio 官网下载官方版，正常安装
2. 员工双击我们的补丁包 cherry-managed-patch.exe
3. 补丁包安装：
   ├── 安装 Sidecar 常驻进程（打通后端链接）
   ├── 写入受管标记
   └── 首次运行注入器（锁死 UI 机制）
4. 完成，员工正常使用（受管版）
```

### 4.2 补丁包内容（轻量，不注入核心功能）
```
cherry-managed-patch.exe（NSIS 安装器）
├── Sidecar 常驻进程        ← 核心（打通后端链接）
├── 注入器（一次性）         ← 锁死 UI 机制（改 asar）
├── 受管标记写入            ← 标记 CherryStudio 为受管
└── 卸载器（控制面板可卸）
```

### 4.3 卸载逻辑
- 移除 Sidecar + 恢复受管标记
- CherryStudio 本体不动（员工还能正常用官方版）
- 控制面板可卸载

### 4.4 核心功能后端拉取（关键）
```
补丁包只装"壳"（Sidecar + 注入器）
        ↓
Sidecar 连上后端后，拉取所有核心功能：
  ├── Agent 模板
  ├── SKILLS 版本
  ├── 模型配置（企_ 前缀）
  ├── 锁死规则（UI 锁死哪些项）
  └── 补丁库（升级自愈用）
        ↓
改后端即可迭代，不重发补丁包
```

---

## 五、锁死 UI 远程迭代（R5 核心）

### 5.1 两层设计：机制固定 + 规则远程拉取
```
锁死 UI = 机制（怎么锁） + 规则（锁什么）
              │                │
         注入器一次到位      后端拉取，可远程迭代
```

| 层 | 内容 | 迭代方式 |
|----|------|---------|
| **机制** | 注入器（把锁死逻辑挂进渲染进程） | 补丁包装一次，基本不动 |
| **规则** | 锁死哪些项/隐藏哪些/只读哪些（JSON 配置） | **后端拉取，随时改** |

### 5.2 运作流程
```
后端 Web后台 改「锁死规则.json」
        ↓
员工端 Sidecar 定时/实时拉取新规则
        ↓
注入器把新规则应用到 CherryStudio UI
        ↓
员工端锁死界面更新（无需重装）
```

### 5.3 举例
- 今天锁死"模型设置页"，明天想加锁"MCP 设置页" → 后端改一行配置，员工端自动生效

---

## 六、升级自愈（R7 核心）

### 6.1 影响分层
| 组件 | 是否受 CherryStudio 升级影响 | 原因 |
|------|---------------------------|------|
| **Sidecar（独立进程）** | ✅ 不受影响 | 独立进程，不依赖程序文件 |
| **后端链接** | ✅ 不受影响 | Sidecar 独立连服务端 |
| **UI 锁死补丁（注入 asar）** | ❌ 会被覆盖 | 官方升级重写程序文件 |

### 6.2 自愈机制（guard.py 完整性校验）
```
员工端 CherryStudio 官方升级
        ↓
Sidecar 检测到版本变化/文件哈希变化（guard.py）
        ↓
Sidecar 从后端补丁库拉取对应版本补丁
        ↓
自动重打 UI 锁死补丁
        ↓
锁死界面恢复（员工无感，无需重装补丁包）
```

### 6.3 版本适配
```
注入器 = 版本适配层
├── 识别 CherryStudio 版本
├── 按版本选择对应补丁（补丁库存多个版本）
└── 打上该版本的 UI 锁死补丁
```
- 新版本 CherryStudio 出来 → 后端补丁库加一个适配补丁
- 员工升级 → Sidecar 自动拉新补丁重打
- **你只需维护后端补丁库，不用管员工端**

---

## 六·B、智能体功能细化（v3.1 新增）

### 6B.1 Git Bash 依赖可见 + 可远程安装（智能体可用性保障）

**背景**：CherryStudio 的智能体依赖 Git Bash 运行（Agent 是 claude-code 类型，需 Git Bash 作为终端环境）。员工端若没装 Git Bash，派发的智能体无法运行。

**机制**：服务端可见（Web后台） + 管理员手动触发远程安装
```
员工端 Sidecar 连接服务端 → 上报 Git Bash 安装状态
        ↓
Web 后台「设备详情」显示：每台设备 Git Bash 是否已装 / 版本
        ↓
管理员看到某台缺 Git Bash → 点「远程安装 Git Bash」按钮
        ↓
服务端下发安装指令（含最新版安装包 URL/内嵌）
        ↓
Sidecar 下载并静默安装 Git Bash 最新版
        ↓
回执安装结果 → 服务端更新设备状态（git_bash_ready: true）
```

**关键区别**：不是服务端全自动检查/安装，而是**服务端能「看到」每台设备的 Git Bash 状态，由管理员决定是否远程安装**。

**实现要点**：
- 设备注册时上报 `git_bash_installed` / `git_bash_version`，Web 后台可见
- Web 后台「设备详情」展示 Git Bash 状态列（已装 v2.x / 未装）
- 管理员点「远程安装 Git Bash」→ 服务端下发指令
- Sidecar 静默安装（Windows 下 Git for Windows 支持 `/VERYSILENT` 参数）
- 安装完成后校验 `git --version`，回执更新状态

### 6B.2 远程派发智能体员工端不可删除（R3 强化）

**需求**：远程派发的智能体，员工端**删除按钮隐藏**，员工无法删除。

**实现**：
- 派发时给 Agent 打「受管标记」（`managed: true`）
- 注入器根据受管标记，在智能体列表/详情页**隐藏删除按钮**（受管 Agent）
- 只有服务端能通过 Sidecar 回收（`dispatch_agent action=delete`）

### 6B.3 远程派发智能体全维度控制（R3 强化）

**需求**：远程派发的智能体，以下全部可由服务端控制：

| 维度 | 内容 | 控制方式 |
|------|------|---------|
| 基础设置 | 模型、名称、描述、图标 | dispatch_agent 更新 |
| 提示词设置 | System Prompt / Instructions | dispatch_agent 更新 |
| 工具 | 工具启用/禁用 | dispatch_agent 更新 |
| 技能 | SKILLS 挂载/版本 | skills 字段 + skills_repo |
| 高级设置 | permission_mode、max_turns 等 configuration | dispatch_agent 更新 |

**实现**：派发的 Agent 完整定义（含全部维度）存服务端 agents_lib，Sidecar 按版本同步，员工端不可改（受管锁定）。

### 6B.4 智能体生产打包链路（Agent 打包 → 上传 → 版本管理 → 下发）

**核心：解决「远程派发 Agent 的数据包从哪来」**

**完整链路**：
```
1. 管理员在本地「调好的 CherryStudio」中调好一个智能体
2. 用我们提供的「打包 SKILLS」让该智能体把自身打包成派发包
3. 打包产物：agent-package.zip（含 agent.json + skills/ + mcp/ + data/）
4. 上传到服务端 Web后台
5. 服务端登记 Agent + 版本号（Agent 版本管理）
6. 服务端下发到目标设备组
7. Sidecar 解包 → 按 agent.json 创建/更新智能体
```

**打包 SKILLS（给调好的智能体用）**：
- 我们交付一个「打包 SKILLS」模板（类似已有 templates/SKILL-mll-review.md）
- 调好的智能体调用该 SKILLS → 自动收集自身配置 + 关联 SKILLS/MCP/数据 → 生成 agent-package.zip

**Agent 包结构（D3 复用）**：
```
agent-package.zip
├── agent.json            # Agent 定义（基础设置/提示词/工具/技能/高级设置）
├── skills/               # 关联 SKILLS 目录（含版本）
├── mcp/                  # MCP 配置
└── data/                 # 预设数据文件
```

**版本管理**：
- 服务端 agents_lib/{agent_name}/{version}/ 存储多版本
- 派发时指定版本，Sidecar 按版本同步
- 更新 = 上传新版本 → 下发；回收 = 移除

**SKILLS 版本管理（D4 强化）**：
- skills_repo/{skill_name}/{version}/ 存储多版本
- Agent 包引用 SKILLS 时指定版本（如 `skill-a@v1.2`）
- 更新 SKILLS = 上传新版本 → 下发到引用它的 Agent

---

## 六·C、设置页管控细化（v3.1 新增）

### 6C.1 远程派发模型平台不可删除（R1/R5 强化）

**需求**：设置页「模型服务 → 添加模型平台」中，远程派发的模型平台**不可删除**。

**实现**：
- 派发模型平台时打「受管标记」（`managed: true`）
- 注入器根据受管标记，隐藏该平台的**删除按钮**（受管模型）
- 员工可查看/使用，但不可删除；只有服务端能通过 dispatch_provider action=remove 收回

### 6C.2 远程派发 SKILLS 不可删除（R4/R5 强化）

**需求**：设置页「技能」中，远程派发的 SKILLS **不可删除**。

**实现**：
- 派发 SKILLS 时打「受管标记」
- 注入器根据受管标记，隐藏该 SKILLS 的**删除按钮**（受管 SKILLS）
- 员工可查看/使用，但不可删除；只有服务端能通过派发回收移除

### 6C.3 受管标记统一机制（R5 强化）

**核心**：所有远程派发项（Agent/模型平台/SKILLS/MCP）统一打 `managed: true` 标记，注入器据此统一处理「锁死/隐藏删除按钮/只读」。

```
受管项（managed: true）
├── 删除按钮隐藏
├── 编辑只读（锁死）
└── Key 隐藏（如适用）
```

---

## 七、服务端（server/）→ 统一调度中枢

```
server/
├── main.py              # 入口，启动 WS 服务 (端口 2334)
├── ws_server.py         # WebSocket 服务端（设备连接管理）
├── device_registry.py   # 设备注册表（在线/离线/状态/依赖）
├── dispatch.py          # 派发调度（Agent/SKILLS/模型 指令下发）
├── web_admin/           # Web 管理后台（浏览器操作）
│   ├── app.py           # Web 后台入口
│   ├── templates/       # 页面模板
│   └── static/          # 静态资源
├── agents_lib/          # Agent 模板库（版本管理）
│   └── {agent_name}/{version}/
│       ├── agent.json   # Agent 定义（基础/提示词/工具/技能/高级设置）
│       ├── skills/      # SKILLS 目录
│       ├── mcp/         # MCP 配置
│       └── data/        # 预设数据文件
├── skills_repo/         # SKILLS 版本仓库
│   └── {skill_name}/{version}/
├── lock_rules.json      # 锁死规则库（远程迭代 UI 锁死）
├── patch_repo/          # 补丁库（按 CherryStudio 版本适配）
│   └── {version}/       # 各版本 UI 锁死补丁
├── gitbash_repo/        # Git Bash 安装包仓库（管理员手动远程安装用）
│   └── Git-latest.exe   # 最新版 Git for Windows
├── providers.json       # 模型仓库（企_ 模型统一配置）
├── data/                # 使用数据存储
└── config.json          # 服务端配置（端口/鉴权）
```

---

## 八、Sidecar（sidecar/）→ 常驻受管客户端

```
sidecar/
├── sidecar.py           # 主进程（常驻，连接服务端）
├── lib/
│   ├── cherry_client.py # 官方 API 客户端（✅ 已有）
│   ├── ws_client.py     # WS 客户端
│   └── sync.py          # 派发/回收执行器
├── collect.py           # 数据采集上报
├── guard.py             # 完整性校验 + 升级自愈
├── injector.py          # 注入器（锁死 UI 机制，补丁包内置）
└── config/sidecar.json  # 本机配置
```

**Sidecar 常驻职责**：
1. 开机自启，连接服务端 WS
2. 收到派发指令 → 调 `cherry_client` 执行（创建/更新/回收 Agent）
3. 收到模型管控指令 → 执行（派发/收回 `企_` 模型）
4. 收到 SKILLS 同步指令 → 写本地数据目录
5. 拉取锁死规则 → 应用到 UI
6. 监控 CherryStudio 升级 → 自愈重打补丁
7. **上报 Git Bash 状态 → Web后台可见；收到安装指令则远程安装**（6B.1）
8. **Agent 包解包 → 创建/更新受管智能体（全维度）**（6B.3/6B.4）
9. 定时上报状态 + 使用数据（按需采集）
10. 断线自动重连

---

## 九、通信协议（WS 消息格式）

### 9.1 设备注册（Sidecar → 服务端）
```json
{
  "type": "register",
  "device_id": "chen-windows-001",
  "hostname": "chen-windows",
  "os": "windows",
  "cherry_version": "2.0.0",
  "git_bash_installed": false,     // v3.1 新增：Git Bash 依赖检测
  "git_bash_version": null,
  "token": "***"
}
```

### 9.2 派发 Agent（服务端 → Sidecar）
```json
{
  "type": "dispatch_agent",
  "action": "create",          // create | update | delete | disable
  "agent": {
    "name": "企_客服助手",
    "managed": true,            // v3.1 新增：受管标记，员工端不可删
    "model": "deepseek:deepseek-v4-flash",
    "instructions": "...",
    "accessible_paths": ["D:\\...\\Agents\\deployed"],
    "configuration": {"permission_mode": "bypassPermissions", "max_turns": 100},
    "tools": ["web_search", "bash"],   // v3.1 新增：工具控制
    "skills": ["skill-a@v1.2", "skill-b@v2.0"]  // v3.1 新增：技能版本控制
  },
  "package_url": "http://server/agents/企_客服助手/v3.zip",  // v3.1 新增：Agent 包下载
  "request_id": "req-001"
}
```

### 9.3 派发模型（服务端 → Sidecar，R1）
```json
{
  "type": "dispatch_provider",
  "action": "add",             // add | remove | update
  "provider": {
    "name": "企_DeepSeek",
    "type": "openai",
    "api_key": "***",          // 经注入器接口注入，员工端不可见
    "base_url": "https://...",
    "models": ["deepseek-v4-flash", "deepseek-v4-pro"]
  },
  "request_id": "req-002"
}
```

### 9.4 锁死规则同步（服务端 → Sidecar，R5）
```json
{
  "type": "sync_lock_rules",
  "rules": {
    "locked_pages": ["provider", "model", "mcp", "skills", "agent"],
    "hidden_keys": true,
    "readonly_fields": ["api_key", "base_url"]
  },
  "version": 3
}
```

### 9.5 补丁拉取（Sidecar → 服务端，R7 自愈）
```json
{
  "type": "fetch_patch",
  "cherry_version": "2.1.0",
  "request_id": "req-003"
}
// 服务端返回对应版本补丁
```

### 9.5·B Git Bash 安装（服务端 → Sidecar，v3.1 新增）
```json
{
  "type": "install_gitbash",
  "url": "http://server/gitbash/Git-latest.exe",  // 最新版安装包
  "request_id": "req-004"
}
// Sidecar 下载静默安装 → 回执
```

### 9.6 执行回执（Sidecar → 服务端）
```json
{
  "type": "dispatch_result",
  "request_id": "req-001",
  "success": true,
  "agent_id": "agent_xxx",
  "error": null
}
```

### 9.7 状态上报（Sidecar → 服务端，定时）
```json
{
  "type": "status",
  "device_id": "chen-windows-001",
  "online": true,
  "agents": ["企_客服助手", "企_设计助手"],
  "cherry_healthy": true,
  "cherry_version": "2.1.0",
  "git_bash_ready": true,      // v3.1 新增
  "last_sync": "2026-08-06T10:00:00Z"
}
```

### 9.8 使用数据上报（Sidecar → 服务端，按需）
```json
{
  "type": "usage",
  "device_id": "chen-windows-001",
  "period": "2026-08-06T09:00:00Z/2026-08-06T10:00:00Z",
  "agent_usage": {
    "企_客服助手": {"sessions": 12, "messages": 45},
    "企_设计助手": {"sessions": 3, "messages": 8}
  },
  "errors": ["..."]
}
```

---

## 十、核心流程

### 10.1 设备上线
```
1. Sidecar 开机自启
2. 连接服务端 WS
3. 发送 register（设备信息 + token + cherry_version + git_bash_installed）
4. 服务端登记设备 → 返回「该设备应有哪些 Agent/SKILLS/模型/锁死规则」清单
5. **Git Bash 状态同步到 Web后台（可见）；管理员可手动触发远程安装**（6B.1）
6. Sidecar 比对本地 → 差异同步（缺的创建，多的回收）
7. 拉取锁死规则 → 应用
8. 进入常驻监听
```

### 10.2 派发新 Agent（Web后台操作）
```
1. 管理员在 Web后台「上传 Agent 包」或「选择已有 Agent 版本」
2. 管理员「派发 Agent 企_客服助手 给 设备组A」
3. 服务端向组内每台 Sidecar 发 dispatch_agent（含 package_url + 受管标记）
4. Sidecar 下载 Agent 包 → 解包 → 调 cherry_client.create_agent() 创建（全维度）
5. 回执 dispatch_result
6. 服务端更新设备状态
```

### 10.3 派发/收回模型（R1，Web后台操作）
```
1. 管理员在 Web后台「派发 企_DeepSeek 给 设备组A」
2. 服务端发 dispatch_provider action=add
3. Sidecar 经注入器接口注入 provider（员工端 UI 锁死不可见）
4. 回执
5. 收回：发 action=remove，Sidecar 移除企_ 前缀项
```

### 10.4 锁死规则迭代（R5，Web后台操作）
```
1. 管理员在 Web后台改「锁死规则」
2. 服务端发 sync_lock_rules
3. Sidecar 拉取新规则 → 应用到 UI
4. 员工端锁死界面更新（无需重装）
```

### 10.5 升级自愈（R7，自动）
```
1. 员工升级 CherryStudio
2. Sidecar 检测版本/哈希变化
3. 从后端补丁库拉取对应版本补丁
4. 自动重打 UI 锁死
5. 恢复受管状态
```

### 10.6 数据采集闭环（R6）
```
1. Sidecar 按需采集使用数据 → 上报
2. 服务端存储 → 分析
3. 分析结果 → 驱动新 Agent/SKILLS 开发
4. 派发回设备
```

---

## 十一、关键设计决策

### D1 企业模型命名隔离（R1）
- 企业派发模型统一加前缀 `企_`（如 `企_DeepSeek`）
- 员工自配模型自由命名，互不干扰
- 收回时从预设层移除 `企_` 前缀项

### D2 Key 保护双保险（R2）
- 强方案：公司模型走**模型网关**，员工端只看到网关 URL，Key 藏网关侧
- 兜底：UI 锁死隐藏 Key 显示

### D3 Agent 完整打包 + 生产链路（R3）
```
agent-package.zip
├── agent.json            # Agent 定义（基础设置/提示词/工具/技能/高级设置）
├── skills/               # SKILLS 目录（含版本）
├── mcp/                  # MCP 配置
└── data/                 # 预设数据文件
```
- **生产链路（6B.4）**：调好的智能体用打包 SKILLS 打包 → 上传服务端 → 版本管理 → 下发
- 服务端版本管理，Sidecar 按版本同步

### D4 SKILLS 版本仓库（R4）
- 复用 NAS 分发思路（SKILL经验蒸馏.md）系统化
- `skills-repo/{name}/{version}/`
- Sidecar 拉取指定版本 → 写员工端 `.claude/skills`
- Agent 包引用 SKILLS 时指定版本（如 `skill-a@v1.2`）

### D5 自进化双路径（R6）
- ① 预设 SKILLS（Agent 自带进化逻辑）
- ② 服务端数据闭环（采集→分析→开发新 Agent/SKILLS 回推）

### D6 设备鉴权
- 每台设备唯一 `device_id` + 简单 token（老板已定）
- 服务端 WS 连接需 token，防未授权设备连入

### D7 派发幂等性
- 每个指令带 `request_id`，Sidecar 执行后回执
- 服务端记录已派发状态，重复派发不重复创建
- 断线重连后，服务端重发未确认指令

### D8 设备分组
- 支持按组派发（如「设计组」「客服组」）
- 组内设备统一收指令

### D9 离线处理
- 设备离线时，指令进入队列
- 设备重连后，服务端补发积压指令

### D10 锁死规则远程化（R5）
- 机制固定（注入器一次到位），规则远程拉取（JSON 配置）
- 改后端即可迭代锁死界面，不重发补丁包

### D11 升级自愈（R7）
- Sidecar 监控版本/哈希变化 → 后端补丁库拉对应版本 → 自动重打
- 放任升级 + 自愈（老板选 A）

### D12 Git Bash 依赖可见 + 可远程安装（6B.1）
- CherryStudio 智能体依赖 Git Bash；设备注册上报 git_bash_installed，Web 后台可见每台状态
- 管理员在 Web 后台看到缺失 → 点「远程安装」→ Sidecar 静默安装最新版

### D13 受管 Agent 不可删除（6B.2）
- 派发 Agent 打 `managed: true` → 注入器隐藏删除按钮
- 只有服务端能回收（dispatch_agent action=delete）

### D14 Agent 全维度控制（6B.3）
- 基础设置/提示词/工具/技能/高级设置全部可由服务端控制
- 员工端受管锁定不可改

### D15 受管项删除保护统一机制（6C）
- 所有远程派发项（Agent/模型平台/SKILLS/MCP）统一打 `managed: true`
- 注入器据此隐藏删除按钮 + 编辑只读
- 只有服务端能回收

### D16 Agent 包上传与版本管理（6B.4）
- 调好的智能体用打包 SKILLS 打包 → 上传 Web后台 → agents_lib 版本管理 → 下发
- 更新 = 上传新版本 → 下发；回收 = 移除

---

## 十二、技术选型

| 组件 | 选型 | 理由 |
|------|------|------|
| 服务端框架 | Python FastAPI + uvicorn | 轻量、异步 WS 支持好、与 Sidecar 同语言 |
| Web 后台 | FastAPI + Jinja2 + 简单前端 | 轻量可视化 |
| WS 库 | `websockets`（服务端）/ `websocket-client`（Sidecar） | 成熟稳定 |
| 数据存储 | SQLite（起步）→ PostgreSQL（规模化） | 起步简单，可扩展 |
| 补丁包 | NSIS 安装器 | 标准 exe，含卸载 |
| Sidecar 常驻 | Python + systemd（Linux）/ NSSM（Windows） | 开机自启、崩溃自愈 |
| 设备注册表 | 内存 + SQLite 持久化 | 在线状态实时，历史持久 |

---

## 十三、交付里程碑（M0-M4）

```
M0 求证验证 → 查源码 + 实测热更新 + 构建 Windows 补丁包
M1 补丁包 → NSIS 安装器（Sidecar + 注入器 + 受管标记 + 卸载）
M2 Sidecar 闭环 → Agent/SKILLS 远程派发+回收+升级（✅ 原型已通）
M3 模型管控 → 企_模型派发/收回 + 员工自配共存 + Key 保护
M4 数据闭环 → 采集→分析→进化 + Web后台完善
```

**v3.1 细化项纳入里程碑**：
- Git Bash 状态可见 + 管理员手动远程安装 → M1（sidecar/gitbash.py + Web后台）
- Agent 打包 SKILLS + 上传链路 → M2（tools/package-skill/）
- 受管标记 + 删除按钮隐藏（Agent/模型/SKILLS）→ M1 注入器 + 后端
- Agent 全维度控制 → M2

### 当前进度
- ✅ **M2 原型已通**：Sidecar 骨架 + 客户端库 + 官方 API 派发闭环（创建/更新/回收 Agent 全通，commit 77d4bd9）
- ✅ **方案 v1.0/v2.0/v3.0/v3.1 已落盘**
- ⏳ **M0 待做**：热更新实测 + Windows 补丁包构建验证
- ⏳ **M1/M3/M4 待做**

---

## 十四、已确认决策点（老板拍板）

| # | 决策 | 内容 |
|---|------|------|
| 1 | 锁死 UI | 选 2（彻底锁死 UI），避免员工乱改导致不稳定 |
| 2 | 服务端部署 | 本机 |
| 3 | 服务端 WS 端口 | **2334** |
| 4 | 设备鉴权 | 简单 token |
| 5 | 数据采集 | 全量数据，但**按需采集**（不主动采集，调用分析时才采集；内部合规） |
| 6 | 实施节奏 | 先跑通再拓展；先出方案再出开发文档 |
| 7 | 模型命名 | `企_` 前缀隔离，员工自配不受影响 |
| 8 | 热更新 | 硬要求，必须热更新（待实测） |
| 9 | 交付形态 | **补丁安装包**（员工装官方版 + 打补丁） |
| 10 | 控制端 | **Web 管理后台** |
| 11 | 补丁包定位 | **只打通后端链接，不注入核心功能**；核心功能后端拉取 |
| 12 | 升级策略 | **A：放任升级 + 自愈**（Sidecar 监控 + 后端补丁库 + 自动重打） |
| 13 | Git Bash 依赖 | CherryStudio 智能体依赖 Git Bash；**服务端可见每台设备是否已装，缺则管理员可远程装最新版**（6B.1） |
| 14 | Agent 不可删 | 远程派发智能体删除按钮隐藏，仅服务端可回收（6B.2） |
| 15 | Agent 全维控制 | 基础设置/提示词/工具/技能/高级设置全部可控制（6B.3） |
| 16 | Agent 打包链路 | 调好的智能体用打包 SKILLS 打包 → 上传服务端 → 版本管理 → 下发（6B.4） |
| 17 | 模型平台不可删 | 远程派发模型平台删除按钮隐藏，仅服务端可收回（6C.1） |
| 18 | SKILLS 不可删 | 远程派发 SKILLS 删除按钮隐藏，仅服务端可回收（6C.2） |

---

## 十五、待求证 / 阻塞项

- [ ] **M0-热更新实测**（阻塞）：需要一台 Windows 测试机装 CherryStudio，实测 provider/Agent 改完是否不重启生效
- [ ] **M0-补丁包构建**：验证 Linux 环境能否出 Windows 补丁包（NSIS/Wine 跨平台坑）
- [ ] **M0-注入器可行性**：验证注入器能否改 asar 锁死 UI（Electron 41）
- [ ] **R1 模型管控执行细节**：注入器需暴露 provider 注入接口（或直写 sqlite 热更新），待 M3 实测
- [ ] **M1+ 各里程碑实现**

---

## 十六、交付物结构

```
cherry-managed/                          ← 交付根目录
├── server/                              ← 管理服务端
│   ├── main.py                          ← WS 服务入口 (2334)
│   ├── ws_server.py                     ← 设备连接管理
│   ├── device_registry.py               ← 设备注册表
│   ├── dispatch.py                      ← 派发调度
│   ├── web_admin/                       ← Web 管理后台
│   ├── lock_rules.json                  ← 锁死规则库
│   ├── patch_repo/                      ← 补丁库（按版本）
│   ├── gitbash_repo/                    ← Git Bash 安装包仓库
│   ├── providers.json                   ← 模型仓库
│   ├── agents_lib/                      ← Agent 模板库（版本管理）
│   ├── skills_repo/                     ← SKILLS 版本仓库
│   ├── data/                            ← 使用数据存储
│   └── config.json                      ← 服务端配置
├── patch/                               ← 补丁包（NSIS 安装器）
│   ├── installer.nsi                     ← NSIS 脚本
│   ├── sidecar/                          ← Sidecar 打包
│   ├── injector/                         ← 注入器
│   └── dist/                             ← 生成的 exe
├── sidecar/                             ← 员工端常驻进程
│   ├── sidecar.py                       ← 受管客户端主进程
│   ├── lib/
│   │   ├── cherry_client.py             ← 官方 API 客户端（✅ 已有）
│   │   ├── ws_client.py                 ← WS 客户端
│   │   └── sync.py                      ← 派发/回收执行器
│   ├── collect.py                       ← 数据采集上报
│   ├── guard.py                         ← 完整性校验 + 升级自愈
│   ├── gitbash.py                       ← Git Bash 状态上报/远程安装（v3.1）
│   ├── injector.py                      ← 注入器（锁死 UI 机制）
│   └── config/sidecar.json              ← 本机配置
├── tools/                               ← 打包 SKILLS（v3.1 新增）
│   └── package-skill/                   ← 智能体打包 SKILLS（调好的智能体用它打包）
│       └── SKILL.md                     ← 打包流程（收集配置+SKILLS/MCP/数据→生成 zip）
├── docs/
│   ├── 方案-企业受管版-v3.1.md           ← 本文档
│   ├── 架构.md                           ← 分层图
│   └── 部署手册.md                       ← 员工安装步骤
└── README.md
```

---

## 十七、与旧文档的关系

| 旧文档 | 状态 | 说明 |
|--------|------|------|
| `docs/方案-企业受管版-v1.0.md` | 保留 | 历史版本 |
| `docs/方案-企业受管版-v2.0.md` | 保留 | 历史版本，v3.x 整合其内容 |
| `docs/方案-企业受管版-v3.0.md` | 保留 | 历史版本，v3.1 整合其内容 |
| `docs/方案-企业受管版-v3.1.md` | **当前** | 本文件 |
| `docs/方案A-服务端Sidecar远程闭环.md` | 作废 | 片面方案，被 v2.0/v3.x 取代 |
| `docs/开发文档A0-A1-服务端骨架与单机派发.md` | 保留 | 开发规格，M2 部分实现依据 |
| `docs/INDEX.md` | 保留 | 入口 |
| `docs/SKILL经验蒸馏.md` | 保留 | 经验存档 |
