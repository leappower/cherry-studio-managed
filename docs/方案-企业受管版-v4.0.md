# CherryStudio 企业受管版 · 最终方案 v4.0

> **版本**：v4.0（最终方案 / 定案版）
> **日期**：2026-08-07
> **前置**：方向定案（跟随官方最新 + Fork 适配）、4+4 轮质询收敛（质询记录-v4.0.md）、源码校验 v2.0.1（源码校验报告-v2.0.1.md）
> **性质**：最终方案。架构方向已拍板，本文档完整落地 R1-R7，不再质疑架构，聚焦可执行性。
> **文档链**：任务分解-v4.0.md（任务清单）→ 本文档（方案成品）→ 质询记录-v4.0.md（收敛过程）→ 源码校验报告-v2.0.1.md（技术地基）

---

## 〇、v4.0 相对 v3.1 的核心变化（方向反转）

| 维度 | v3.1（旧，已废弃） | v4.0（最终定案） |
|------|------------------|-----------------|
| 员工端版本 | **锁定 v1.9.x**（老板旧拍板） | **跟随官方最新**（老板重拍板，推翻） |
| 管理通道 | 依赖官方 `/v1/agents` HTTP API | **Fork 源码加 `/v1/admin/*` 管理路由**（官方无管理路由，实锤） |
| R1+R2 | **模型网关**（曾提升为主方案） | **直连 provider 派发 key + 花费监控异常停 key 重建**（老板否决网关） |
| 适配策略 | 锁死小版本免适配 | **官方更新 → 我们适配最新版**（增量 patch + 按需升级） |
| 数据存储 | v1 localStorage（渲染注入改 redux） | **v2 sqlite**（Fork 管理路由直写，R1 更简单） |
| 受管标记 | 注入器（v1 无列） | **旁路 managed_registry**（Fork 泛化受管保护，不动官方 schema） |
| 锁死 UI | 注入器改 asar | **Fork 源码级改渲染组件**（更稳，代价每版 rebase） |
| Fork 层地位 | 与 Sidecar 并列 | **核心层**（管理路由 + 锁死 + 更新接管，Sidecar 依赖的前提） |

**核心认知**：v2 的 provider/agent 存 sqlite（非 v1 的 localStorage），Fork 管理路由直接写 sqlite，R1 模型管控比 v1 简单一个数量级；代价是**持续适配官方高频发版**，靠「增量 patch + 自动构建流水线 + 按需升级节奏」控制维护成本。

---

## 一、需求定义（R1-R7）

### R1 · 模型统一管控
- **定义**：公司统一管理员工可用的公司模型，可派发、可收回。
- **强化点**：
  1. 远程端派发**公司模型列表**（`企_` 前缀隔离）；
  2. 可**收回**（删除受管 provider 或禁用其 key）；
  3. **员工仍可自配其他模型不受影响**（双向并存，员工自配不在 managed_registry 中，完全自由）。
- **实现层**：Fork 管理路由 `/v1/admin/providers` + Sidecar 派发 + managed_registry 隔离。

### R2 · Key 保护
- **定义**：员工端**看不到**公司派发模型的 Key 原文。
- **实现方式**（定案：不用模型网关）：
  1. **UI 锁死**：Fork 源码级隐藏公司受管 provider 的 apiKey 字段显示，员工无法查看/编辑；
  2. **兜底**：服务端**花费监控**实时聚合，异常（超阈值/超速/单设备超限）→ **停 key 重建**（中转侧停用旧 key、新建 key、Fork 管理路由只更新受管 provider 的 key 字段）→ 热更新。
- **诚实边界**：无模型网关时，key 必然物理存在于员工端 sqlite（user_provider 表）。R2 落地为「UI 不可见 + 异常停 key 兜底」，**做不到物理隔离**（已如实向老板说明并获认可）。
- **中转侧边界（老板新拍板）**：中转 = 第三方中转服务商（如 OpenAI 兼容中转平台），key 归中转侧持有，Fork 管理路由**无法控制中转侧 key 停用**；停 key 走中转侧对接通道（中转侧 API 停用/授权接口，否则人工兑底），Fork 仅更新员工端 provider 的 key 字段（详见 7.4）。

### R3 · Agent 远程管理
- **定义**：远程派发 Agent **开箱即用**，支持**收回 / 禁用 / 升级**。
- **Agent = MCP + SKILL + 预设数据文件**完整打包（agent-package.zip）。
- **实现层**：Fork 管理路由 `/v1/admin/agents`（对接 AgentService 完整 CRUD）+ Sidecar 派发。

### R4 · SKILLS 派发 + 版本管理
- **定义**：支持 **Bootstrap、SKILLS 版本、MCP**，可回收 / 升级。
- **实现层**：Fork 管理路由 `/v1/admin/skills` + `/v1/admin/mcp` + 服务端 skills_repo（版本仓库）+ Sidecar 派发。

### R5 · 锁死 UI
- **定义**：员工端设置页，远程端管理的（模型 / MCP / SKILLS / Agent）**锁死或隐藏**；**锁死规则可远程迭代**。
- **实现层**：Fork 源码级改渲染组件加 `managed` 判断（隐藏删除按钮 / 只读）+ `lock_rules.json` 远程迭代（机制固定 + 规则远程拉取）。

### R6 · 数据采集（对整个智能体工作目录管理）
- **定义**：Sidecar 采集 → 服务端分析。**采集范围 = 整个智能体工作目录**（Agent 的上下文、产出内容、数据文件全部在内），不只用量计数。这是合规的，因为 CherryStudio 是企业工作软件，公司对员工工作产生的上下文与产出拥有管理权。
- **实现层**：
  1. Fork 管理路由 `/v1/admin/usage` 读员工端 `ai_usage_record` 表（模型+token 用量，供花费监控/设备健康）；
  2. **工作目录采集**：通过 Agent 的 `accessible_paths`（Agent 可访问路径，方案 4.2/6.2 已定义）读取智能体工作目录内的上下文与产出内容（对话记录、生成文件、数据文件等），Fork 加 `/v1/admin/agent-files` 路由（按 agent_id/路径枚举+读取）→ Sidecar 按需拉取上报服务端数据仓库；
  3. 采集触发：**按需**（服务端发起/排查时），非主动常驻上报。
- **合规边界（老板纠正 2026-08-07）**：采集含**上下文与产出内容**（工作目录全量），非仅用量；因 CherryStudio 为工作软件，公司有权管理员工工作产出。传输走 WS 2334（可升级 WSS/TLS）；服务端加密存储；采集范围限 Agent 工作目录（`accessible_paths` 内），**不越权读取工作目录之外的个人文件**。

### R7 · 升级免疫
- **定义**：员工自己升级**不破坏受管状态**；官方更新后我们的 Fork 自动适配。
- **实现层（定案）**：
  1. **禁用官方更新通道**：Fork 改 `feedURL`（electron-updater generic provider，从 `https://releases.cherry-ai.com` 指向自建更新通道）；
  2. **Fork 增量 patch**：官方原版 + fork diff，官方更新 rebase managed 分支；
  3. **按需升级节奏**：重要功能 / 安全修复才追官方新版（触发标准见 D19）；
  4. **构建流水线**：GitHub Actions windows-latest 自动构建 → 推自建更新通道 → Sidecar 轮询升级。

---

## 二、三层架构详细设计

```
┌─────────────────────────────────────────────────────────────────────┐
│  服务端 server/  (FastAPI · 端口 2334)                                │
│  ├── Web 管理后台（管理员登录鉴权 + 操作审计日志）                      │
│  ├── 花费监控模块（ai_usage_record 聚合看板 + 异常告警 + 停 key 重建）  │
│  ├── agents_lib / skills_repo / lock_rules / gitbash_repo / patch_repo│
│  ├── 设备注册表 + 设备分组 + 派发幂等（request_id）                    │
│  └── 数据仓库（员工端用量聚合存储）                                    │
└──────────▲───────────────────────────────────────▲───────────────────┘
           │ WS(2334) 长连接                          │ WS(2334) 长连接
┌──────────┴────────────┐                ┌──────────┴────────────┐
│ 员工设备 A             │      ...      │ 员工设备 N             │
│ ┌──────────────────┐  │                │ ┌──────────────────┐  │
│ │ Fork 层（改官方    │  │                │ │ Fork 层          │  │
│ │ 最新 cherry-studio）│  │                │ │（同 A）          │  │
│ │  /v1/admin/* 路由  │  │                │ └────────▲─────────┘  │
│ │  独立管理 key      │  │                │          │ HTTPS       │
│ │  泛化受管保护      │  │                │ ┌────────┴─────────┐  │
│ │  热更新 IPC 广播   │  │                │ │ Sidecar (NSSM)   │  │
│ │  锁死 UI（源码级） │  │                │ │  WS 客户端        │  │
│ │  禁官方更新通道    │  │                │ │  派发/采集/对账    │  │
│ └────────▲─────────┘  │                │ │  managed_registry │  │
│          │ HTTPS 127.0.0.1             │ │  自愈             │  │
│ ┌────────┴─────────┐  │                │ └──────────────────┘  │
│ │ Sidecar (NSSM)   │  │                └───────────────────────┘
│ │  WS 客户端 + Fork │  │
│ │  管理路由客户端    │  │
│ └──────────────────┘  │
└───────────────────────┘
```

### 2.1 Fork 层（改官方最新版 cherry-studio 本体）— 核心

职责：为 Sidecar / 服务端提供受管管理通道，锁死员工 UI，接管更新。

| 模块 | 职责 | 落地要点 |
|------|------|---------|
| **管理路由 `/v1/admin/*`** | agents/providers/skills/mcp/usage 的 CRUD + 读取 | 在官方 `v1Routes` Elysia 插件链追加 `adminRoutes`；复用 AgentService/ProviderService 方法，**不直写 sqlite**（规避写门控/锁库/事务风险） |
| **独立管理 key** | `feature.api_gateway.managed_key`，与普通 API key（`feature.api_gateway.api_key`）分离 | Sidecar 持管理 key，加密存储不落明文；管理路由走独立 bearer 鉴权 |
| **泛化受管保护** | 把官方硬编码 `cherryai`（`assertManagedCherryAiProviderMutationAllowed` / `isManagedCherryAiProviderId`）泛化为 managed_registry 判定 | 受管 provider/agent 在管理路由与渲染层均不可改/删；员工自配（不在 registry）完全自由 |
| **热更新 IPC 刷新广播** | 管理路由写 sqlite 后，向渲染进程广播「数据已变更请刷新」 | 写 sqlite → IPC 广播 → 渲染进程 TanStack Query `invalidateQueries('/agents')` 刷新 UI（复用官方 useDataApi 机制） |
| **锁死 UI（源码级）** | 渲染组件加 `managed` 判断：隐藏删除按钮 / 受管项只读 / 隐藏 apiKey 显示 | 改 Fork 渲染组件（Sessions.tsx 删除 `canDelete`、Provider 删除 `useProviderDelete`、key 显示字段等） |
| **禁用官方更新通道** | 改 `feedURL` 指向自建更新通道 | `electron-builder.yml` publish 改 generic provider url → 自建 feed；`autoUpdater.channel` 接管 |
| **usage 管理路由** | 读 `ai_usage_record` 表（providerId/modelId/inputTokens/outputTokens/totalTokens/sourceType） | 供 Sidecar 定时拉取上报 |

### 2.2 Sidecar 层（独立进程，NSSM Windows 服务）— 常驻执行器

职责：连服务端 WS + 调 Fork 管理路由 + 派发 + 采集 + 对账 + 自愈。

| 模块 | 职责 | 落地要点 |
|------|------|---------|
| **WS 客户端** | 连服务端 2334，长连接，心跳 | 断线重连 + 指数退避；重连后补传未确认指令 |
| **Fork 管理路由客户端** | 调本地 Fork `/v1/admin/*`（HTTPS 127.0.0.1） | 用独立管理 key 鉴权 |
| **派发执行器** | 派发 Agent/模型/SKILLS（幂等） | 每指令带 request_id，执行后回执；断线重发不重复创建 |
| **usage 采集上报** | 定时拉 `/v1/admin/usage` → 汇总上报服务端 | 低频（5 分钟）；字段白名单（用量） |
| **工作目录采集** | 按服务端指令拉 `/v1/admin/agent-files`（Agent 工作目录上下文+产出）→ 上报 | 限 accessible_paths 内，不越权读取工作目录外个人文件 |
| **对账** | 调 `list_agents` 比对服务端期望清单 | 缺的补、受管的保护、非受管的忽略；结果上报（Web 后台可见不一致告警） |
| **受管标记旁路表** | 本地 `managed_registry.db`（sqlite），Sidecar 唯一写者 | 记录 id→managed 映射；不动官方 schema，无迁移锁库风险 |
| **自愈** | Fork 升级失败回滚上一版；注入失败重跑 | 保留上一版备份；Sidecar 与 Fork 解耦互不影响 |
| **NSSM 服务注册** | Windows 服务，开机自启，崩溃自愈 | 与 Fork 解耦；CherryStudio 未启动时 Sidecar 照常连服务端（报 cherry_online:false） |

### 2.3 服务端（server/ · 端口 2334 · FastAPI）— 统一调度中枢

| 模块 | 职责 |
|------|------|
| **Web 管理后台** | 管理员登录（账号鉴权）+ 操作审计日志（谁在何时做了何操作）；设备/Agent/模型/SKILLS/锁死规则/花费看板管理 |
| **花费监控模块** | `ai_usage_record` 聚合看板 + 异常告警（阈值/增速/单设备超限）+ 停 key 重建流程 + **中转服务对接**（中转侧 key 停用/授权通道） |
| **agents_lib** | Agent 模板库 + 版本管理（Agent 打包上传 → 下发） |
| **skills_repo** | SKILLS 包上传 + 版本管理 + 下发 |
| **lock_rules** | 锁死规则生成 + 下发（机制固定 + 规则远程迭代） |
| **gitbash_repo** | Git Bash 安装包托管 + 远程安装指令 |
| **patch_repo** | Fork 安装包 + 补丁库（增量 patch） |
| **设备注册表** | device_id + token + 状态（在线/离线）+ 分组 |
| **数据仓库** | 员工端用量聚合存储（SQLite 起步 → PostgreSQL 规模化） |

---

## 三、管理路由 API 设计（`/v1/admin/*`）

> 鉴权：独立管理 key（`feature.api_gateway.managed_key`），bearer 比对（timing-safe）。与普通 API key 分离，员工拿到普通 key 也无法调管理路由。
> 实现：在官方 `v1Routes` Elysia 插件链追加 `adminRoutes` 插件，复用各 Service 公开方法，不直写 sqlite。

### 3.1 Agent 管理（对接 AgentService CRUD）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/v1/admin/agents` | 创建 Agent（createAgentWithId），body 含 name/type/model/instructions/configuration/tools/skills，`managed:true` 写入旁路表 |
| GET | `/v1/admin/agents` | 列出全部 Agent（listAgents），含 managed 标记 |
| GET | `/v1/admin/agents/:id` | 查看单个（getAgent） |
| PUT | `/v1/admin/agents/:id` | 全量更新（updateAgent），受管项校验 |
| DELETE | `/v1/admin/agents/:id` | 删除（deleteAgent）——受管保护：非 managed_registry 写者不可删受管项 |
| POST | `/v1/admin/agents/:id/disable` | 禁用（软删/下架），员工端不可见不可用 |
| POST | `/v1/admin/agents/reorder` | 排序（reorder） |

### 3.2 Provider / 模型管理（对接 ProviderService CRUD）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/v1/admin/providers` | 创建 provider（create），含 base_url/type/models |
| POST | `/v1/admin/providers/batch-upsert` | 批量写入（batchUpsert） |
| PUT | `/v1/admin/providers/:id` | 更新（update）——受管保护：只更新受管 provider，不动员工自配 |
| DELETE | `/v1/admin/providers/:id` | 删除（delete）——受管项仅服务端可删 |
| POST | `/v1/admin/providers/:id/api-keys` | 加 key（addApiKey）——**停 key 重建流程用** |
| PUT | `/v1/admin/providers/:id/api-keys` | 替换 key（replaceApiKeys）——**重建后推新 key** |
| DELETE | `/v1/admin/providers/:id/api-keys` | 删 key（deleteApiKey）——**仅删员工端 provider 的 key 字段**（key 归中转侧持有，Fork 无法停用中转侧 key，见 7.4） |

### 3.3 SKILLS / MCP 管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST/PUT/DELETE | `/v1/admin/skills` | agent_skill 表 CRUD + 版本写 agent_global_skill |
| GET/POST/PUT/DELETE | `/v1/admin/mcp` | mcp_server 表 CRUD |

### 3.4 Usage / 工作目录读取（供 Sidecar 采集）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/v1/admin/usage` | 读 `ai_usage_record` 表（providerId/modelId/inputTokens/outputTokens/totalTokens/sourceType），支持时间窗/设备过滤 |
| GET | `/v1/admin/agent-files` | 枚举+读取智能体工作目录（accessible_paths 内）上下文与产出内容，按 agent_id/路径过滤——R6 工作目录采集通道 |

### 3.5 受管保护语义（泛化）

- `isManaged(id)` → 查旁路表 managed_registry；
- 管理路由对受管项的变更请求需校验请求方为 Sidecar/服务端（管理 key 本身就保证）；
- 渲染层对受管项隐藏删除/编辑（见第五节锁死 UI）；
- 员工手动改（绕过管理路由）由 Sidecar 对账发现并修复。

---

## 四、WS 通信协议（服务端 ↔ Sidecar，端口 2334）

> 服务端 WS 框架：`websockets`；Sidecar 客户端：`websocket-client`。JSON 消息，`type` 区分。

### 4.1 设备注册（Sidecar → 服务端）
```json
{
  "type": "register",
  "device_id": "chen-windows-001",
  "hostname": "chen-windows",
  "os": "windows",
  "cherry_version": "2.0.1",
  "fork_version": "4.0.0-rc.1",
  "git_bash_installed": false,
  "git_bash_version": null,
  "token": "***"
}
```

### 4.2 派发 Agent（服务端 → Sidecar）
```json
{
  "type": "dispatch_agent",
  "action": "create",
  "agent": {
    "name": "企_客服助手",
    "managed": true,
    "model": "企_DeepSeek:deepseek-v4-flash",
    "instructions": "...",
    "accessible_paths": ["D:\\...\\Agents\\deployed"],
    "configuration": {"permission_mode": "bypassPermissions", "max_turns": 100},
    "tools": ["web_search", "bash"],
    "skills": ["skill-a@v1.2", "skill-b@v2.0"]
  },
  "package_url": "http://server/agents/企_客服助手/v3.zip",
  "request_id": "req-001"
}
```

### 4.3 派发模型 Provider（服务端 → Sidecar，R1）
```json
{
  "type": "dispatch_provider",
  "action": "add",
  "provider": {
    "name": "企_DeepSeek",
    "type": "openai",
    "api_key": "***",
    "base_url": "https://...",
    "models": ["deepseek-v4-flash", "deepseek-v4-pro"]
  },
  "request_id": "req-002"
}
```

### 4.4 锁死规则同步（服务端 → Sidecar，R5）
```json
{
  "type": "sync_lock_rules",
  "rules": {
    "locked_pages": ["provider", "model", "mcp", "skills", "agent"],
    "hidden_keys": true,
    "readonly_fields": ["api_key", "base_url"]
  },
  "version": 3
}
```

### 4.5 Usage 上报（Sidecar → 服务端，定时 / 按需）
```json
{
  "type": "usage",
  "device_id": "chen-windows-001",
  "period": "2026-08-07T09:00:00Z/2026-08-07T10:00:00Z",
  "records": [
    {"provider": "企_DeepSeek", "model": "deepseek-v4-flash",
     "input_tokens": 1200, "output_tokens": 800, "total_tokens": 2000}
  ],
  "errors": []
}
```

### 4.6 执行回执（Sidecar → 服务端）
```json
{
  "type": "dispatch_result",
  "request_id": "req-001",
  "success": true,
  "agent_id": "agent_xxx",
  "error": null
}
```

### 4.7 状态上报（Sidecar → 服务端，定时心跳）
```json
{
  "type": "status",
  "device_id": "chen-windows-001",
  "online": true,
  "agents": ["企_客服助手", "企_设计助手"],
  "cherry_healthy": true,
  "cherry_online": false,
  "cherry_version": "2.0.1",
  "fork_version": "4.0.0-rc.1",
  "git_bash_ready": false,
  "last_sync": "2026-08-07T10:00:00Z"
}
```

### 4.8 补丁 / Git Bash 安装（服务端 → Sidecar）
```json
{ "type": "fetch_patch", "cherry_version": "2.1.0", "request_id": "req-003" }
{ "type": "install_gitbash", "url": "http://server/gitbash/Git-latest.exe", "request_id": "req-004" }
```

### 4.9 消息幂等与可靠性
- 每条派发指令带 `request_id`，服务端记录已派发状态，Sidecar 回执后确认；
- 断线重连后，服务端重发未确认指令（幂等，不重复创建）；
- 离线设备指令入队，重连后补发。

---

## 五、热更新机制设计（IPC 刷新广播链路）

**技术前提（M0-1 实测）**：官方 v2 的 Agent/Provider 管理走 DataApi（渲染进程调 IPC → Service 写 sqlite → 返回值驱动 UI），**官方没有「服务端/Sidecar 侧写 sqlite → 渲染 UI 实时刷新」的现成链路**。渲染进程用 TanStack Query（`useDataApi`），数据刷新靠 `invalidateQueries('/agents')`。因此 Fork 必须**自建「写入 → UI 刷新」链路**。

### 5.1 链路（方案 A）
```
Fork 管理路由（服务端/Sidecar 侧）
   │ 写 sqlite（复用 AgentService/ProviderService 方法，触发官方内部变更事件）
   ▼
主进程 补 IPC 刷新广播（新增 channel，如 `data-changed`，携带刷新目标）
   ▼
渲染进程 监听广播 → 对相应 query key 调 invalidateQueries('/agents' | '/providers' | ...)
   ▼
TanStack Query 重新拉取 → UI 即时刷新（不重启）
```

### 5.2 验收标准（M0-1 必测）
1. 改 Agent 名/提示词 → UI 即时更新；
2. 派发 provider → 模型下拉即时可选；
3. 停 key 重建推新 key → 受管 provider 生效，员工端无需重启。

### 5.3 兜底（方案 C）
- 若 IPC 广播链路实测做不出（M0-1 失败），**降级为「强制重启兜底」**：管理路由写完 sqlite 后，触发 CherryStudio 主进程重启（或提示员工重启）；
- 热更新为老板硬要求，方案 A 优先；方案 C 仅作技术兜底，需如实汇报。

### 5.4 一致性
- 管理路由写完 sqlite 主动广播 UI 刷新，UI 短暂延迟（秒级）可接受；
- Sidecar 周期性对账兜底，修复 UI 与服务端不一致。

---

## 六、锁死 UI 设计（源码级 managed 判断 + lock_rules 远程迭代）

### 6.1 两层设计
- **机制固定（源码级）**：Fork 渲染组件加 `managed` 判断，受管项隐藏删除按钮 / 只读 / 隐藏 apiKey。此机制在 Fork 源码编译期固定，随 Fork 版本更新。
- **规则远程迭代**：`lock_rules.json` 从服务端拉取，控制「哪些页面锁 / 隐藏哪些字段 / 只读哪些字段」等**策略参数**（非机制本身）。改后端即可迭代锁死范围，不重发 Fork 包。

### 6.2 源码级 managed 判断注入点（v2.0.1 已核清）

> ⚠️ **注入点基线提醒（v4.0 补充）**：下表注入点基于当前 v2.0.1 源码核清，官方 UI 重构会导致注入点漂移。**实施时（F-10）必须重新拉取 M0-3 的最新官方基线，逐项重新核对下述注入点行号/组件路径，不直接套用下表现状行号**。若官方已重构相关组件，以下表为起点重新定位等价注入点。
| 位置 | 官方现状 | Fork 改法 |
|------|---------|----------|
| Agent 删除按钮 | `Sessions.tsx` `canDelete={!!workspaceId}` | 加 `&& !isManaged(agent.id)` |
| Agent 编辑 | Agent 配置页可改 | 受管项只读（disabled） |
| Provider 删除 | `useProviderDelete.ts` → `deleteProviderById` | 受管 provider 不显示删除入口 |
| Provider Key 显示 | 渲染层显示 apiKey 原文 | 受管 provider 隐藏/掩码 key 字段（R2） |
| SKILLS / MCP 删除 | 设置页可删 | 受管项隐藏删除入口 |
| Model 下拉 | provider.models 可选 | 受管 provider 模型列表由服务端控制 |

### 6.3 受管判定
- 渲染层经 preload 暴露的 IPC 读旁路表 `managed_registry.db`（Sidecar 唯一写者）；
- `isManaged(id)` 查旁路表；受管项（在 registry）→ 锁死；员工自配（不在 registry）→ 完全自由。

---

## 七、花费监控模块详细设计（R2 兜底核心）

### 7.1 数据源
- **权威数据源**：员工端 `ai_usage_record` 表（v2 原生记录每条 AI 调用的 providerId/modelId/inputTokens/outputTokens/totalTokens/sourceType）；
- Fork 加 `/v1/admin/usage` 路由读出；Sidecar 定时拉取 → 上报服务端数据仓库；
- **判定口径**：以员工端上报的用量计数为基准（**老板决策 A 已确认**走员工端表，而非中转账单）。
- **中转侧边界（老板新拍板）**：中转 = 第三方中转服务商（如 OpenAI 兼容中转平台）。因此 **key 归中转侧持有**，Fork 管理路由**无法控制中转侧的 key 停用**（`/v1/admin/providers/:id/api-keys` 只能更新员工端 provider 的 key 字段，管不到中转侧）。停 key 需设计中转侧对接通道（授权/停用接口）或人工兑底（详见 7.4）。

### 7.2 聚合看板（Web 后台）
- 按 provider / model / 设备 / 时间聚合；
- 展示：总 token、估算花费（单价表配置）、设备排行、模型排行；
- 异常高亮（红色标记触发阈值项）。

### 7.3 异常判定规则（多维度，防误判）
| 规则 | 说明 | 触发 |
|------|------|------|
| **单设备超限** | 某设备累计 token/花费超设定阈值 | 阈值可配（如单设备日花费 > X 元） |
| **增速异常** | 某设备/模型 token 增速超正常波动（环比/同比） | 增速 > 设定倍数 |
| **总额超限** | 全公司某 provider/model 累计超预算 | 预算阈值 |
| **关键模型异常** | 某企_模型被高频调用（疑似滥用） | 调用频次超限 |

### 7.4 停 key 重建流程
```
1. 触发异常（单设备超限/增速异常/总额超限）
2. 服务端告警（Web 后台高亮 + 通知管理员）
3. 管理员确认（人工确认重建，防误判）
4. 停旧 key：**走中转侧对接通道停用**该设备对应的企_ provider key
   └── 中转侧对接通道（老板拍板：key 归第三方中转侧持有，Fork 管不到中转侧 key）：
       ├── 优先：中转侧管理 API（若第三方提供 /admin 停用/授权接口，经服务端中转对接模块调用）
       └── 兜底：人工在中转平台停用（无管理 API 时）
   ⚠️ Fork `/v1/admin/providers/:id/api-keys` 只能更新员工端 provider 的 key 字段，**不能停用中转侧 key**
5. 新建 key：中转侧新建 key
6. 推新 key：Fork `/v1/admin/providers/:id/api-keys` PUT replaceApiKeys
   └── 只更新受管 provider 的 key 字段，不动员工自配（推的是中转侧新 key，使员工端指向新 key）
7. 热更新：IPC 广播 → 受管 provider 生效
8. 回执 + 审计日志
```

### 7.5 降级策略
- 停 key 期间员工暂时用自配模型（R1 双向并存的天然缓冲）；
- 进行中会话中断（可接受，异常滥用场景）；
- 重建为手动确认（人工判断），不自动盲目停 key；
- **中转侧停 key 兑底**：若中转侧无管理 API（不能自动停用），停 key 退化为「人工在中转平台停用 + 服务端生成停工单向管理员并回填确认」，停用完成前该设备企_ provider 可能仍可用——如实记录该兑底窗口。

### 7.6 与 R6 采集的关系
- 花费监控**复用**员工端 `ai_usage_record`（同一数据源，模型+token 用量）；
- **R6 采集 = 工作目录全量管理**：除用量计数外，还采集 Agent 工作目录内（`accessible_paths`）的上下文与产出内容——对话记录、生成文件、数据文件，经 Fork `/v1/admin/agent-files` 路由按需读取上报；
- 花费监控（用量）与工作目录采集（内容）走同一套采集上报链路（Sidecar + WS 2334），字段/范围不同但统一由服务端控制触发；
- 合规依据：CherryStudio 为企业工作软件，公司对员工工作产出的管理权覆盖上下文与产出内容（老板 2026-08-07 纠正）。

---

## 八、设计决策（D1-D20）

> 含 v3.1 决策 D1-D16 + 本次拍板新增 D17-D20。

### 8.1 v3.1 既有决策（保留，方向已适配）
- **D1 企业模型命名隔离（R1）**：`企_` 前缀隔离，员工自配自由；真正控制靠 managed_registry。
- **D2 Key 保护（R2）**：**改为**「UI 锁死隐藏 + 花费监控异常停 key 重建」（见 D17），不再走网关。
- **D3 Agent 完整打包 + 生产链路（R3）**：agent-package.zip（agent.json + skills/ + mcp/ + data/）。
- **D4 SKILLS 版本仓库（R4）**：`skills-repo/{name}/{version}/`，Agent 包引用 `skill-a@v1.2`。
- **D5 自进化双路径（R6）**：①预设 SKILLS；②服务端数据闭环。
- **D6 设备鉴权**：每设备唯一 device_id + token；WS 连接需 token。
- **D7 派发幂等性**：request_id + 回执 + 断线重发。
- **D8 设备分组**：按组派发（设计组/客服组）。
- **D9 离线处理**：离线指令入队，重连补发。
- **D10 锁死规则远程化（R5）**：机制固定 + 规则远程拉取。
- **D11 升级免疫（R7）**：**改为**「禁用官方 feed + Fork 增量 patch + 自建更新通道」（见 D18/D19/D20）。
- **D12 Git Bash 依赖可见 + 可远程安装（6B.1）**：bundled MinGit 兜底 git 命令；完整 Git Bash 缺失可远程装。
- **D13 受管 Agent 不可删除（6B.2）**。
- **D14 Agent 全维度控制（6B.3）**。
- **D15 受管项删除保护统一机制（6C）**：Agent/模型/SKILLS/MCP 统一 managed 判定（旁路表）。
- **D16 Agent 包上传与版本管理（6B.4）**。

### 8.2 v4.0 新增/重拍板决策（本次定案）

- **D17 花费监控停 key 兜底（R2，老板决策 A，推翻网关）**：
  - **不用模型网关**（老板否决：维护成本高、简单事情复杂化）；
  - R1+R2 靠：直连 provider 派发 key + **花费监控异常停 key 重建**兜底；
  - 花费数据源走员工端 `ai_usage_record`（v2 原生，Fork 加 `/v1/admin/usage` 读取），Sidecar 上报；
  - **中转 = 第三方中转服务商**（老板新拍板）：key 归中转侧持有，Fork 管理路由无法控制中转侧 key 停用（只能更新员工端 provider 的 key 字段）；停 key 需设计中转侧对接通道（中转侧 API 停用/授权接口）或人工兑底，见 7.4。

- **D18 跟随官方最新 + Fork 适配（R7，老板决策 B，推翻锁定 v1.9.x）**：
  - 员工端用**官方最新版**，Fork 加管理路由/锁死/更新接管；
  - 官方更新 → 我们适配最新版（Fork 增量 patch + 按需升级）；
  - 分支策略：`official/main`（官方镜像）+ `managed/xxx`（Fork 改动），官方更新 rebase managed 分支。

- **D19 按需升级触发标准（R7）**：
  - 仅以下情况追官方新版：**①安全修复 ②重要功能（影响受管能力/热更新）③官方关键 bug**；
  - 普通功能更新不追，保持 Fork 稳定；待老板确认标准后固化。

- **D20 Fork 管理路由走 Service 不直写 sqlite（质询 A15 收敛）**：
  - 管理路由**复用 AgentService/ProviderService 公开方法**（createAgentWithId/updateAgent/addApiKey/replaceApiKeys 等），不直写 sqlite；
  - 规避 drizzle/libsql 单连接 WAL 写门控、锁库/损坏风险；仅官方无方法时才补，且遵循同样事务模式。

---

## 九、里程碑 M0-M4（引用任务分解-v4.0.md）

| 里程碑 | 内容 | 交付判据 | 关键任务 |
|--------|------|---------|---------|
| **M0** | 求证验证 | 热更新链路通 + Windows 包可构建 + 官方最新基线 + 管理路由可写 | M0-1 热更新实测；M0-2 Windows 构建；M0-3 拉官方最新基线；M0-4 管理路由对接验证；M0-5 ai_usage_record 读取 |
| **M1** | Fork 层完成 | `/v1/admin/*` 全路由 + 独立 key + 受管保护 + 热更新 + 锁 UI + 禁更新通道 | F-1~F-12 |
| **M2** | Sidecar 闭环 | 设备注册 + 派发 Agent/模型/SKILLS + 对账 + usage 上报 + 自愈 | S-1~S-10 |
| **M3** | 服务端 + 花费监控 | Web 后台 + 花费看板 + 异常停 key + 派发调度 | D-1~D-10 |
| **M4** | 构建发布 + 端到端 | 安装包 + 自建更新通道 + 全链路验收 | E-1~E-4 |

**关键路径**：M0-3（官方基线）→ F-1（Fork 分支）→ F-2/F-4/F-5（管理路由）→ M0-1（热更新验证）→ F-9（热更新广播）→ M1 → S-1~6 → M2 → D-6（花费监控）→ M3。

---

## 十、技术选型

| 组件 | 选型 | 理由 |
|------|------|------|
| 服务端框架 | Python FastAPI + uvicorn | 轻量、异步 WS 支持好、与 Sidecar 同语言 |
| Web 后台 | FastAPI + Jinja2 + 简单前端 | 轻量可视化 |
| WS | `websockets`（服务端）/ `websocket-client`（Sidecar） | 成熟稳定 |
| 数据存储 | SQLite（起步）→ PostgreSQL（规模化） | 起步简单可扩展 |
| Sidecar 常驻 | Python + NSSM（Windows 服务） | 开机自启、崩溃自愈、与 Fork 解耦 |
| Sidecar 打包 | PyInstaller → 独立 Windows exe | 员工机无 Python，需独立 exe |
| Fork 构建 | GitHub Actions windows-latest runner + electron-builder（NSIS） | 本机 Linux 无 Wine/Windows 打包链，走 CI |
| 更新通道 | electron-updater generic provider（自建 feed） | 替代官方 releases.cherry-ai.com |
| Fork 语言/栈 | 跟随官方（Electron + Elysia + drizzle + libsql + TanStack Query） | 最小 diff，随官方栈 |

---

## 十一、风险与对策

| 风险 | 等级 | 对策 |
|------|------|------|
| **热更新链路做不出（M0-1）** | 🔴 高 | 方案 A 复用 invalidateQueries；失败降级方案 C（强制重启兜底），如实汇报 |
| **Fork 适配成本（官方高频发版）** | 🟡 中 | 增量 patch + 按需升级（D19 触发标准）+ 自动 rebase 构建流水线 |
| **管理路由写 sqlite 破坏事务** | 🟡 中 | 复用 AgentService/ProviderService（D20），不直写 |
| **花费监控误判异常** | 🟡 中 | 多维度规则（阈值/增速/单设备超限）+ 人工确认重建 |
| **Key 物理隔离做不到（R2 边界）** | 🟡 中 | 已如实说明：无网关时 key 必存员工端；UI 隐藏 + 异常停 key 兜底；若后续需物理隔离再评估网关 |
| **Windows 包构建（Linux 本机）** | 🟡 中 | GitHub Actions windows-latest runner 自动构建 |
| **Sidecar 单点（CherryStudio 未启动）** | 🟡 中 | Sidecar 独立常驻，报 cherry_online:false，指令入队，启动后执行 |
| **数据一致性（员工手动改 vs 受管）** | 🟡 中 | Sidecar 周期性对账（缺的补/受管保护/非受管忽略），Web 后台可见告警 |
| **Web 后台鉴权缺失** | 🟡 中 | 管理员账号登录 + 操作审计日志 + 设备 token 可轮换 + request_id 追踪 |
| **官方 UI 重构致锁死注入点漂移** | 🟡 中 | 锁死 UI 走 Fork 源码级（编译期固定），随 Fork rebase；规则参数走 lock_rules 远程化 |

---

## 十二、交付物结构

```
cherry-managed/                          ← 交付根目录
├── server/                              ← 管理服务端（端口 2334）
│   ├── main.py                          ← WS 服务入口
│   ├── ws_server.py                     ← 设备连接管理
│   ├── device_registry.py               ← 设备注册表 + 分组
│   ├── dispatch.py                      ← 派发调度（幂等）
│   ├── cost_monitor/                    ← 花费监控模块（v4.0 新增）
│   │   ├── aggregator.py                ← ai_usage_record 聚合看板
│   │   ├── anomaly.py                   ← 异常判定规则
│   │   ├── key_rotation.py              ← 停 key 重建流程
│   │   └── transit_bridge/              ← **中转服务对接组件（v4.0 补充）**
│   │       ├── transit_client.py        ← 中转侧管理 API 客户端（停用/授权接口，若第三方提供）
│   │       ├── transit_manual.py        ← 人工兜底：无管理 API 时生成停用工单向管理员，回填确认
│   │       └── README.md                ← 中转侧对接说明（第三方管理 API 支持矩阵 + 停用流程）
│   ├── web_admin/                       ← Web 管理后台（鉴权+审计）
│   ├── agents_lib/                      ← Agent 模板库（版本管理）
│   ├── skills_repo/                     ← SKILLS 版本仓库
│   ├── lock_rules/                      ← 锁死规则库
│   ├── gitbash_repo/                    ← Git Bash 安装包
│   ├── patch_repo/                      ← Fork 安装包 + 增量 patch
│   ├── data/                            ← 数据仓库
│   └── config.json                      ← 服务端配置
├── fork/                                ← Fork 源码（改官方最新 cherry-studio）
│   ├── official/                        ← 官方镜像分支
│   ├── managed/                         ← Fork 改动分支
│   └── patches/                         ← 增量 patch
├── sidecar/                             ← 员工端常驻进程（NSSM）
│   ├── sidecar.py                       ← 主进程
│   ├── lib/
│   │   ├── ws_client.py                 ← WS 客户端
│   │   └── fork_client.py               ← Fork 管理路由客户端
│   ├── dispatch.py                      ← 派发执行器（幂等）
│   ├── collect.py                       ← usage 采集上报 + 工作目录采集（agent-files）
│   ├── reconcile.py                     ← 对账
│   ├── managed_registry.py              ← 受管标记旁路表
│   ├── selfheal.py                      ← 自愈
│   └── config/sidecar.json              ← 本机配置
├── tools/                               ← 打包工具
│   └── package-agent/                   ← Agent 打包（agent.json+skills+mcp+data）
├── docs/
│   ├── 方案-企业受管版-v4.0.md           ← 本文档（最终方案）
│   ├── 任务分解-v4.0.md                 ← 任务清单
│   ├── 质询记录-v4.0.md                 ← 收敛过程
│   └── 源码校验报告-v2.0.1.md           ← 技术地基
└── README.md
```

---

## 十三、验收标准与验收方案（逐里程碑可执行判据）

> 验收原则：每个里程碑验收 = **具体测试动作 + 通过条件 + 验收方式**，不依赖主观判断。热更新、锁死 UI、花费监控等老板硬要求有单独验收项。验收由品控官（shencha）执行，通过后推进看板。

### 13.1 M0 求证验收（前置阻塞项）

| ID | 验收项 | 测试动作 | 通过条件 | 方式 |
|----|--------|---------|---------|------|
| V-M0-1 | 热更新链路通 | 测试机跑 Fork，改 Agent 名/提示词 + 派发 provider | UI 不重启即时更新；模型下拉即时可选 | 实测 188 测试机 |
| V-M0-2 | Windows 包可构建 | GitHub Actions windows-latest 跑 electron-builder | 产出 NSIS 安装包，可安装启动 | CI 验证 |
| V-M0-3 | 官方最新基线已拉 | cherry-src 拉 origin/main，建 managed 分支 | 分支存在，rebase 干净无冲突 | git 验证 |
| V-M0-4 | 管理路由可写 sqlite | 调 /v1/admin/agents POST 创建 | 创建成功，事务/校验不破坏，UI 刷新 | 实测 |
| V-M0-5 | ai_usage_record 可读 | 调 /v1/admin/usage | 读出模型+token 用量，字段完整 | 实测 |

**M0 总判据**：V-M0-1 至 V-M0-5 全绿；任一项红则阻塞对应后续任务。V-M0-1（热更新）红时按 5.3 降级方案 C 重评。

### 13.2 M1 Fork 层验收

| ID | 验收项 | 测试动作 | 通过条件 |
|----|--------|---------|---------|
| V-M1-1 | /v1/admin/* 全路由 | 逐条调 agents/providers/skills/mcp/usage/agent-files CRUD | 全返回 200，数据正确写入 sqlite |
| V-M1-2 | 独立管理 key | 用普通 API key 调管理路由 | 401 拒绝；用管理 key 200 |
| V-M1-3 | 受管保护 | 用非受管写者改/删受管项 | 被拒（错误码明确） |
| V-M1-4 | 热更新广播 | 管理路由写库后 | UI 即时刷新（复用 V-M0-1） |
| V-M1-5 | 锁死 UI | 受管 provider/agent 删除按钮 | 隐藏/禁用；员工自配项不受影响 |
| V-M1-6 | 禁官方更新 | 改 feedURL + autoUpdate 配置 | 员工点升级不连官方 releases.cherry-ai.com |

### 13.3 M2 Sidecar 闭环验收

| ID | 验收项 | 测试动作 | 通过条件 |
|----|--------|---------|---------|
| V-M2-1 | 设备注册 | Sidecar 启动连 WS 2334 | 服务端设备注册表出现，状态在线 |
| V-M2-2 | 派发 Agent | 服务端派 dispatch_agent | 员工端 Agent 出现，可收回/禁用/升级 |
| V-M2-3 | 派发模型 | 服务端派 dispatch_provider | 模型下拉出现，key 生效，员工看不到 key 原文 |
| V-M2-4 | 对账 | 员工手动改/删受管项 | Sidecar 对账修复，Web 后台可见告警 |
| V-M2-5 | usage 上报 | 定时拉 /v1/admin/usage 上报 | 服务端数据仓库有记录 |
| V-M2-6 | 工作目录采集 | 服务端拉 /v1/admin/agent-files | 读出 Agent 工作目录上下文+产出，限 accessible_paths 内 |
| V-M2-7 | 自愈 | 模拟 Fork 升级失败 | 回滚上一版，Sidecar 不崩 |
| V-M2-8 | 断线重连 | 断开 WS 再连 | 重连成功，指令幂等不重复创建 |

### 13.4 M3 服务端 + 花费监控验收

| ID | 验收项 | 测试动作 | 通过条件 |
|----|--------|---------|---------|
| V-M3-1 | Web 后台登录鉴权 | 未登录访问管理页 | 重定向登录；操作有审计日志 |
| V-M3-2 | 花费看板 | 造 token 数据 | 按 provider/model/设备聚合正确，异常高亮 |
| V-M3-3 | 异常告警 | 触发单设备超限/增速异常 | 告警触发，Web 高亮 + 通知管理员 |
| V-M3-4 | 停 key 重建 | 管理员确认后走 7.4 流程 | 中转侧停旧 key → 建新 key → 推员工端新 key → 热更新生效 |
| V-M3-5 | 中转对接 | 调 transit_bridge | 有管理 API 走自动停用；无 API 生成停工单人工兑底 |
| V-M3-6 | 派发调度 | 分组派发 + 幂等 | 按组下发，断线重发不重复创建 |

### 13.5 M4 端到端验收（全链路）

| ID | 验收项 | 测试动作 | 通过条件 |
|----|--------|---------|---------|
| V-M4-1 | 安装包集成 | 全新 Windows 装 Fork 包 | 安装成功，Sidecar 自动注册，受管标记生效 |
| V-M4-2 | 更新通道 | 发新版本触发升级 | 员工端自动从自建 feed 升级，受管状态不丢 |
| V-M4-3 | 全链路冒烟 | 跑 R1-R7 七条主链路（见下） | 7/7 通过 |
| V-M4-4 | 卸载三态 | 分别测三种卸载 | 符合选定语义（见 14.2） |

### 13.6 R1-R7 端到端验收清单（V-M4-3 展开）

| 需求 | 端到端验收动作 | 通过条件 |
|------|---------------|---------|
| R1 | 派发企_模型→员工用→收回 | 员工端模型出现→可用→消失；员工自配不受影响 |
| R2 | 派发 key→员工查看 | 员工看不到 key 原文；异常触发停 key 重建成功 |
| R3 | 派发 Agent→收回/禁用/升级 | 完整打包链路通，三个操作均生效 |
| R4 | 上传 SKILLS→版本管理→下发 | 员工端技能出现，可回收/升级 |
| R5 | 下发锁死规则→迭代 | 受管项锁死/隐藏，规则改后远程生效 |
| R6 | 服务端拉工作目录采集 | 读出上下文+产出，限 accessible_paths 内 |
| R7 | 官方更新→Fork 适配→升级 | 员工升级不破坏受管状态，适配成功 |

### 13.7 验收状态追踪

- 每里程碑验收结果记入看板任务（品控官 shencha 评分）；
- 验收不通过 → 打回对应层修复，修复后复验；
- 热更新（V-M0-1/V-M1-4）为老板硬要求，不达标不进入下一里程碑。

---

## 十四、待老板最终确认项（非阻塞，方案已完整）

1. **升级触发标准（D19）**：按需升级的具体触发条件固化（重要功能/安全修复/关键 bug）——需老板拍板标准。
2. **卸载语义**：①移除受管（清 Fork 注入+Sidecar，保留数据）②完全清理（含受管 Agent）③回滚（恢复官方版）。默认建议①，员工离职场景需完全清理——需老板定。
3. **数据仓库存储选型**：SQLite 起步已够，规模化是否迁 PostgreSQL——按数据量演进。

---

## 十五、与旧文档的关系

| 文档 | 状态 |
|------|------|
| `docs/方案-企业受管版-v4.0.md` | **当前（最终方案）** |
| `docs/方案-企业受管版-v3.1.md` | 保留（历史，方向已改，Fork 层结论承接） |
| `docs/方案-企业受管版-v1.0/v2.0/v3.0.md` | 保留（历史） |
| `docs/任务分解-v4.0.md` | 保留（任务清单，本方案落地依据） |
| `docs/质询记录-v4.0.md` | 保留（收敛过程，方向定案记录） |
| `docs/源码校验报告-v2.0.1.md` | 保留（技术地基，v4.0 关键技术事实来源） |
| `docs/方案A-服务端Sidecar远程闭环.md` | 作废（片面方案，被 v4.0 取代） |
| `docs/开发文档A0-A1-服务端骨架与单机派发.md` | 保留（开发规格，服务端基础） |