# 密钥与敏感信息
list.json
*.key
*.pem
.env

# Python
__pycache__/
*.pyc
.venv/
.venv-build/
venv/

# 构建产物
dist/
build/
node_modules/

# 系统
.DS_Store
Thumbs.db

# CherryStudio API key 本地副本
APIkey.txt

# SQLite 运行时数据（不入库）
server/data/*.db
server/data/*.db-wal
server/data/*.db-shm
.pytest_cache/
server/.pytest_cache/

# 自建更新通道 feed（运行时生成，不入库）
server/patch_repo/
